# Проектирование доменной области (этап 1)
## Бизнес-концепция и сценарии

**Продукт:** Samrt Space - платформа для бронирования рабочих мест и переговорок в коворкинге.
**Цель:** Автоматизировать процесс поиска свободного пространства и его бронирования, исключив двойные брони

## Основные сценарии (Use Case):

1. **Поиск и просмотр:** пользователь фильтрует переговорки по вместимости и оборудованию в них и смотрит расписание
2. **Бронирование:** пользователь выбирает слот времени и оплачивает или подтверждает свою бронь
3. **Отмена:** пользователь отменяет бронь в соответствии с правилами возврата
4. **Администрирование:** админ добавляет новые переговорки и меняет их статус (например, "На ремонте")

## Доменные модели (сущности):

<img width="658" height="797" alt="image" src="https://github.com/user-attachments/assets/76af7594-7932-44da-8d9a-7f2d6c4d9030" />


## Бизнес-правила и ограничения:

1. **Запрет двойного бронирования:** нельзя забронировать *Space*, если перекрывается время (*StartTime* и *EndTime*) с уже существующей активной бронью.
2. **Валидация времени:** *StartTime* должно быть в будущем. *EndTime* должно быть строго больше *StartTime*. Минимальный интервал между бронированием должен составлять 30 минут (время, за которое работники коворкинга будут убирать переговорку)
3. **Статус пространства:** нельзя забронировать *Sapce*, если его *Status* != *Active*
4. **Отмена:** отменить бронь можно не позднее, чем за 1 час до *StartTime*
5. **Расчет стоимости**: 
   - Каждая комната имеет тариф `pricePerHour` (цена за час)
   - Стоимость брони = `pricePerHour × количество часов`
   - Минимальная длительность брони — 30 минут (оплачивается как полный час)

6. **Оплата** (упрощенная модель):
   - В рамках данного проекта оплата не реализуется
   - Цена рассчитывается автоматически для информирования пользователя
